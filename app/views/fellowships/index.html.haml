%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.fellowships.each do |fellowship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(fellowship.followed_user.email.downcase)}?s=40")
            = link_to fellowship.followed_user.full_name, fellowship.followed_user
          %td.extra-padding= fellowship.followed_user.queue_items.count
          %td.extra-padding= fellowship.followed_user.followers.count
          %td.extra-padding
            = link_to fellowship_path(fellowship), method: :delete do
              %i.glyphicon.glyphicon-remove
